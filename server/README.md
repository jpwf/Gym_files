# Gestao_byte - Backend

Servidor Node.js básico usando Express.

Instalação e execução:

```bash
cd server
npm install
npm run dev
```

Endpoints de exemplo:

- `GET /` -> mensagem de status
- `GET /api/health` -> { status: 'ok' }
- `GET /api/hello` -> { hello: 'world' }
